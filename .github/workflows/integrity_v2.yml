name: Verify Athena Canon, Manifest & CAP Payloads

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  civic-integrity:
    runs-on: ubuntu-latest

    steps:
      - name: 🧭 Checkout Athena Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🧩 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 🔐 Install Dependencies
        run: |
          pip install jq jsonschema

      - name: 🔄 Fetch Canonical Manifest from Codex
        run: |
          echo "📦 Fetching FalconForge Canonical Manifest..."
          mkdir -p schemas

          # ✅ Use raw.githubusercontent.com for unencoded manifest content
          if ! curl -fsSL \
            -o schemas/FalconForge_Integrity_Manifest_v3_5.json \
            "https://raw.githubusercontent.com/falconforgeai-rgb/falconforge-codex/main/falconforge-athena-v3_5/canonical/FalconForge_Integrity_Manifest_v3_5.json"; then
            echo "⚠️ Primary fetch failed, retrying with GitHub API fallback..."
            curl -fsSL \
              -H "Accept: application/vnd.github.v3.raw" \
              -o schemas/FalconForge_Integrity_Manifest_v3_5.json \
              "https://api.github.com/repos/falconforgeai-rgb/falconforge-codex/contents/falconforge-athena-v3_5/canonical/FalconForge_Integrity_Manifest_v3_5.json"
          fi

          echo "✅ Manifest fetched successfully."
          head -n 5 schemas/FalconForge_Integrity_Manifest_v3_5.json || echo "⚠️ Manifest preview unavailable."

      - name: 🧮 Verify Schema Hash Against Manifest
        run: |
          echo -e "\033[1;36m🔍 Verifying schema hash integrity...\033[0m"

          MANIFEST_PATH="schemas/FalconForge_Integrity_Manifest_v3_5.json"
          SCHEMA_PATH="schemas/ATHENA_CAP_SCHEMA_v3_5.json"

          if [ ! -f "$MANIFEST_PATH" ]; then
            echo "❌ ERROR: Manifest file missing at $MANIFEST_PATH"
            exit 1
          fi
          if [ ! -f "$SCHEMA_PATH" ]; then
            echo "❌ ERROR: Schema file missing at $SCHEMA_PATH"
            exit 1
          fi

          # Normalize UTF-8 encoding to avoid BOM corruption
          iconv -f utf-8 -t utf-8 "$MANIFEST_PATH" -o "$MANIFEST_PATH"

          echo "📖 Reading manifest entry for ATHENA_CAP_SCHEMA_v3_5.json"

          # 🧩 Diagnostic echo – show all entries matching schema name
          echo "🧾 Found manifest entries for schema:"
          jq -r '[.modules[]?, .files[]?, .artifacts[]?, .records[]?] 
            | map(select(.name=="ATHENA_CAP_SCHEMA_v3_5.json"))' "$MANIFEST_PATH"

          # 🧩 Improved jq logic to handle multiple entries & empty fields
          MANIFEST_HASH=$(jq -r '
            [
              (.modules[]?, .files[]?, .artifacts[]?, .records[]?) |
              select(.name=="ATHENA_CAP_SCHEMA_v3_5.json") |
              (.sha256 // .hash // .checksum // empty)
            ]
            | map(sub("SHA256:";"") | gsub(" ";"")) 
            | map(select(length > 0))
            | last // empty
          ' "$MANIFEST_PATH")

          # Compute local hash
          RUNTIME_HASH=$(sha256sum "$SCHEMA_PATH" | awk '{print $1}')

          echo "Manifest Hash : $MANIFEST_HASH"
          echo "Runtime  Hash : $RUNTIME_HASH"

          if [ -z "$MANIFEST_HASH" ]; then
            echo "⚠️ Manifest entry not found or invalid for ATHENA_CAP_SCHEMA_v3_5.json"
            exit 1
          fi

          if [ "$MANIFEST_HASH" != "$RUNTIME_HASH" ]; then
            echo -e "\033[1;31m❌ Integrity mismatch detected!\033[0m"
            echo "A schema file has drifted from canonical baseline."
            echo "🔎 Computing diff for inspection..."
            diff -u <(head -n 20 "$SCHEMA_PATH") \
                   <(grep -A20 ATHENA_CAP_SCHEMA_v3_5.json "$MANIFEST_PATH" || true) || true
            exit 1
          fi

          echo -e "\033[1;32m✅ Integrity verified — hashes match.\033[0m"

      - name: 🧠 Validate CAP Payload Schema
        run: |
          echo -e "\033[1;36m🔍 Validating CAP payload structure...\033[0m"
          python - <<'PYCODE'
          import json, jsonschema, sys
          try:
              with open("schemas/ATHENA_CAP_SCHEMA_v3_5.json") as schema_file:
                  schema = json.load(schema_file)
              with open("cap_record.json") as data_file:
                  data = json.load(data_file)
              jsonschema.validate(instance=data, schema=schema)
              print("✅ CAP payload structure is valid.")
          except FileNotFoundError as e:
              print(f"❌ Missing file: {e.filename}")
              sys.exit(1)
          except jsonschema.ValidationError as e:
              print("❌ CAP payload validation failed:")
              print(e.message)
              sys.exit(1)
          except json.decoder.JSONDecodeError as e:
              print(f"❌ JSON parsing error in schema or payload: {e}")
              sys.exit(1)
          except Exception as e:
              print(f"❌ Unexpected error: {e}")
              sys.exit(1)
          PYCODE

      - name: 🪶 Archive Integrity Logs
        if: always()
        run: |
          mkdir -p archive/CAP_LOGS
          LOG_FILE="archive/CAP_LOGS/integrity_$(date +%Y%m%d_%H%M%S).log"
          {
            echo "Integrity validation completed at $(date -u)"
            echo "Manifest path: $MANIFEST_PATH"
            echo "Schema path: $SCHEMA_PATH"
          } > "$LOG_FILE"
          echo "🪶 Result stored in $LOG_FILE"
